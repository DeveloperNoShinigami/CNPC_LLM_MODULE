// modules/irons_spells/ai_goals/maintain_distance.js — AI Goal: maintain_distance
//
// Self-registers with GoalsLoader when loaded via load().

GoalsLoader.registerGoal(
  "maintain_distance",
  "Keep a safe casting distance from hostile targets. Retreat if enemies close in. Avoid melee range."
)
