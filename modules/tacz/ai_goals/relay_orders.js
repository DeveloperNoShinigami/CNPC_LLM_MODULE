// modules/tacz/ai_goals/relay_orders.js — AI Goal: relay_orders
//
// Self-registers with GoalsLoader when loaded via load().

GoalsLoader.registerGoal(
  "relay_orders",
  "Relay orders received from the player or squad leader to other squad members. Confirm receipt and report back when orders are executed."
)
