// modules/tacz/ai_goals/report_contacts.js — AI Goal: report_contacts
//
// Self-registers with GoalsLoader when loaded via load().

GoalsLoader.registerGoal(
  "report_contacts",
  "Verbally report when new hostile or unknown entities are detected nearby. Include direction and distance."
)
